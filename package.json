{
  "name": "realestate-microservices",
  "version": "1.0.0",
  "description": "Real estate microservices ecosystem",
  "private": true,
  "workspaces": [
    "ingestor",
    "enrichment",
    "integration-tests"
  ],
  "scripts": {
    "test:ingestor": "cd ingestor && npm test",
    "test:enrichment": "cd enrichment && npm test",
    "test:integration": "cd integration-tests && npm run test:integration",
    "test:all": "npm run test:ingestor && npm run test:enrichment && npm run test:integration",
    "install:all": "npm install && npm run install:services",
    "install:services": "cd ingestor && npm install && cd ../enrichment && npm install && cd ../integration-tests && npm install",
    "build:all": "cd ingestor && npm run build && cd ../enrichment && npm run build",
    "dev:ingestor": "cd ingestor && npm run dev",
    "dev:enrichment": "cd enrichment && npm run dev",
    "docker:up": "cd ingestor && docker compose up -d && cd ../enrichment && docker compose up -d",
    "docker:down": "cd ingestor && docker compose down && cd ../enrichment && docker compose down"
  },
  "devDependencies": {
    "@types/node": "^20.10.4",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
